import { Box } from "@radix-ui/themes";
import { Cart } from "../../components/CartComponent/Cart";

const page = () => {
  return (
    <Box style={{ width: "100%" }}>
      <Cart />
    </Box>
  );
};

export default page;
